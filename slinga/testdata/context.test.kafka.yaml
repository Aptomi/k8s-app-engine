name: test

service: kafka

criteria:
  - dev == "yes" && prod == "no"

labels:
  set:
    some-test-context-specific-label: some-value
  remove:
    a:
    b:

allocations:

  - name: test-{{user.team}}
    criteria:
      - priority >= 2
    labels:
      set:
        some-test-context-specific-label: some-value
      remove:
        a:
        b:

  - name: test-{{user.team}}
    criteria:
      - priority >= 2
    labels:
      set:
        some-test-context-specific-label: some-value
      remove:
        a:
        b:
