// Package engine implements core engine of Aptomi, which does policy resolution (resolve), diff calculation
// between states (diff), and state enforcement by applying actions (apply).
package engine
