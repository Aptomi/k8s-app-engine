// Package progress implements progress indicators, which are used during state enforcement while running actions and
// plugins to estimate progress and show completion percentage to the user.
package progress
