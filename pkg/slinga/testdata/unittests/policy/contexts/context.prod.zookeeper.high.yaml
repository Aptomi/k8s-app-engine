name: prod

service: zookeeper

criteria:
  accept:
    - dev == "no" && prod == "yes" && priority >= 200
  reject:
    - nozookeeper

labels:
  set:
    some-test-context-specific-label: some-value
  remove:
    a:
    b:

allocation:
  name: "prod-{{.User.Name}}"
  labels:
    set:
      some-test-context-specific-label: some-value
    remove:
      a:
      b:
