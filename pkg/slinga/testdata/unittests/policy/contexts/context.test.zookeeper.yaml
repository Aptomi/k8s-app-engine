name: test

service: zookeeper

criteria:
  accept:
    - dev == "yes" && prod == "no"
  reject:
    - nozookeeper

labels:
  set:
    some-test-context-specific-label: some-value
  remove:
    a:
    b:

allocation:
  name: "test-{{.User.Labels.team}}"
  labels:
    set:
      some-test-context-specific-label: some-value
    remove:
      a:
      b:
