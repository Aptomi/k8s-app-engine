name: prod

service: kafka

criteria:
  accept:
    - dev == "no" && prod == "yes" && priority < 200

labels:
  set:
    some-test-context-specific-label: some-value
  remove:
    a:
    b:

allocation:
  name: "prod-{{.User.Labels.team}}"
  labels:
    set:
      some-test-context-specific-label: some-value
    remove:
      a:
      b:
