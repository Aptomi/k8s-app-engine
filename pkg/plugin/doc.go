// Package plugin introduces plugins for Aptomi engine, which are responsible for component instantiation
// and management on different cloud providers (e.g. deployment plugin, such as k8s/helm) and also configuration of
// underlying cloud infrastructure components (e.g. istio plugin).
package plugin
