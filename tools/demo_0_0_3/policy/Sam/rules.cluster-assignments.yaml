- name: dev_cannot_deploy_to_prod_cluster
  enabled: true
  filterservices:
    user:
      accept:
        - org == 'dev'
    cluster:
      accept:
        - prod
  actions:
    - type: dependency
      content: forbid
