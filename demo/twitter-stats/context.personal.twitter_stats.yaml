name: personal

service: twitter-stats

# Who is allowed to access
criteria:
  accept:
    - team == 'platform_services'
  reject:
    - priority < 50
    - compromised

allocations:
  # Each allowed user will have its own instance
  - name: "personal-{{ .User.Name }}"
