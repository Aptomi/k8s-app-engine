# Definition of TweeViz service
name: tweeviz

labels:
  set:
    a: b

components:

  - name: hdfs
    service: hdfs
    labels:
      set: # add/modify
        some-test-context-specific-label: some-value
      remove:
        a:
        b:

  - name: tweeviz
    code:
      type: aptomi/code/kubernetes-helm
      content:
        chart:
          name: boston-summit-charts/tweeviz
        params:
          hdfs.externalAddress: hdfs://hdfs-namenode:8020/{{ .User.Name }}
    labels:
      set: # add/modify
        some-test-context-specific-label: some-value
      remove:
        a:
        b:
    dependencies:
      - hdfs
