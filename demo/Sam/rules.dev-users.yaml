- name: dev_users_forbid_prod_clusters
  enabled: false
  filterservices:
    user:
      accept:
        - org == 'dev'
    cluster:
      accept:
        - prod
  actions:
    - type: dependency
      content: forbid
