name: prod

service: kafka

labels:
  set:
    antiAffinity: hard

allocations:
  # Dedicated for high-priority users
  - name: "personal-{{ .User.Name }}"
    criteria:
      accept:
        - priority >= 200

  # Shared for the rest of the team
  - name: "team-{{ .User.Labels.team }}"
    criteria:
      accept:
        - priority < 200
