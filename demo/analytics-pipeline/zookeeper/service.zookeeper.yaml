# Definition of Zookeeper service
name: zookeeper

components:

  - name: zookeeper
    code:
      type: aptomi/code/kubernetes-helm
      metadata:
        chartName: zookeeper-1.0.0
      params:
        replicas: 1
        antiAffinity: "{{ .Labels.antiAffinity }}"
        image:
          repository: frostman/zookeeper
          tag: 3.5.3-rc1
