name: test

service: hdfs

criteria:
  accept:
    - dev == "yes" || qa == "yes"

labels:
  set:
    antiAffinity: soft

allocations:

  - name: test-org12
    criteria:
      accept:
        - org == "org1" || org == "org2"

  - name: test-not-org12
    criteria:
      accept:
        - org != "org1" && org != "org2"
