name: test

service: hdfs

criteria:
  - dev == "yes" || qa == "yes"

labels:
  set:
    antiAffinity: soft

allocations:

  - name: test-org12
    criteria:
      - org == "org1" || org == "org2"

  - name: test-not-org12
    criteria:
      - org != "org1" && org != "org2"
