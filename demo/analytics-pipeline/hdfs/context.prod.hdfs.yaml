name: prod

service: hdfs

criteria:
  accept:
    - prod == "yes"

allocations:

    # shared inside organisation
  - name: "prod-{{ .User.Labels.org }}"
    labels:
      set:
        antiAffinity: hard
