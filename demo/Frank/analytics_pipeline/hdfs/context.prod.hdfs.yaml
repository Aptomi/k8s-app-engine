name: prod

# This context describes hdfs in production
service: hdfs

# This context is for people from it/analytics-ops-team (they control production instance)
criteria:
  accept:
    - org == 'it' && is_operator

# In production:
# - service will be deployed in a cluster specified by label
# - as a single production instance
allocation:
  name: "production"
